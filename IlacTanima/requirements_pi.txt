# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# ── Camera — picamera2 is installed system-wide via apt (apt install python3-picamera2)
# ── OpenCV headless (no GUI, smaller) ─────────────────────────────────────────
opencv-python-headless>=4.8.0

# ── Detection / OCR / Barcode ─────────────────────────────────────────────────
easyocr>=1.7.0
pyzbar>=0.1.9
rapidfuzz>=3.6.0

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.24.0
Pillow>=10.0.0
requests>=2.31.0

# ── Optional: only needed for model conversion on dev machine, NOT on Pi ──────
# ultralytics>=8.3.0
